# Netlify Redirects for Telugu Delicacies
# -----------------------------------------------------------------------------
# 1. Essential SEO files (serve as is)
/sitemap.xml    /sitemap.xml    200
/robots.txt     /robots.txt     200

# 2. Redirect old QR code links to root
/sales/*    /:splat    301!

# 3. Fallback all other unknown routes to sales.html to handle product rendering
/*          /sales.html    200
